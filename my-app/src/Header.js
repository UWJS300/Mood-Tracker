import React from 'react'

import './Header.css'

const Header = (props) => {
  return (
    <header>
      <h1>
        <span className='car-word'>Mood Tracker</span>
      </h1>
    </header>
  )
}

export default Header;
